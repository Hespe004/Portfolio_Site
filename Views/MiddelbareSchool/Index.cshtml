@model IEnumerable<Portfolio_Site.Models.MiddelbareSchool>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    Deze kunnen worden aangemaakt bij Portfolio Eigenaren
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opleiding)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Begin_Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Eind_Datum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opleiding)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Begin_Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Eind_Datum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
